@model IberaDelivery.Models.CreditCardForm
@inject IHttpContextAccessor HttpContextAccessor
@using System.Text.Json
@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Credit Card Information</h4>
<hr />
<div class="row">
    @using (Html.BeginForm("Create", "CreditCard"))
    {
        <div style="color:red;">@Html.ValidationSummary()</div>
        <div class="col-lg-12">@ViewBag.Message</div>
        <div class="col-md-6">
            <div class="row">
                <div class="col-lg-2">@Html.LabelFor(a => a.TargetNumber)</div>
                <div class="col-lg-10">@Html.TextBoxFor(a => a.TargetNumber, new { @class = "form-control" })</div>
            </div><br />
            <div class="row">
                <div class="col-lg-2">@Html.LabelFor(a => a.Cardholder)</div>
                <div class="col-lg-10">@Html.TextBoxFor(a => a.Cardholder, new { @class = "form-control" })</div>
            </div><br />
        </div>
        <div class="col-md-6">
            <input type="submit" value="Create" class="btn btn-info" />
        </div>
    }
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>