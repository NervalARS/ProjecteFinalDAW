@model IEnumerable<IberaDelivery.Models.Order>
@inject IHttpContextAccessor HttpContextAccessor
@{
    ViewData["Title"] = "Orders";
    
}

<h2>Productes</h2>

<a asp-action="Create" class="btn-info"><button>New Product +</button></a>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Import)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>LnOrder</th>
            <th>Delete</th>
            <th>Edit</th>
            <th>Details</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th>
                @Html.DisplayFor(modelItem => item.Id)
            </th>
            <th>
                @Html.DisplayFor(modelItem => item.Date)
            </th>
            <th>
                @Html.DisplayFor(modelItem => item.Import)
            </th>
            <th>
                @Html.DisplayFor(modelItem => item.UserId)
            </th>
            
         @*   @if (@HttpContextAccessor.HttpContext.Session.GetString("userName") != null){  *@

            
                <td>
                   <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
                <td>
                   <a asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                </td>
                <td>
                   <a asp-action="Details" asp-route-id="@item.Id">Details</a>
                </td>
                <td>
                   <a asp-action="LnOrder" asp-route-id="@item.Id">LnOrder</a>
                </td>
       @*     }else{
                <td></td>
                <td></td>
            } *@
            </tr>
        }
    </tbody>
</table>

<form asp-action="Index">
<input type="text" class="form-control" name="Cadena"> <input type="button" value="Cerca"> @ViewBag.missatge
</form>
@if(@ViewBag.missatge != null){
<a asp-action="Index">Treure filtre</a>
}

